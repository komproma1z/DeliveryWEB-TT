{
    "name": "vkauth",
    "private": true,
    "version": "0.0.0",
    "description": "VK access app",
    "devDependencies": {
        "http-server": "^0.9.0"
    },
    "scripts": {
        "prestart": "npm install",
        "start": "http-server ./app -a localhost -p 4000 -c-1"
    }
}
